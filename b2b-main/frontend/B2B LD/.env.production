# Production build: frontend is served at /b2b/ on the VPS, and nginx
# proxies /b2b/api/ to the B2B backend — so API calls stay same-origin,
# relative to /b2b, avoiding CORS entirely. Not a secret, safe to commit.
VITE_API_BASE_URL=/b2b
