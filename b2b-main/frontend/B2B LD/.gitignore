# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment / secrets
.env
.env.*.local
!.env.example
# .env.production is tracked on purpose — it's just the public /b2b base path,
# no secrets, and makes the VPS build reproducible without extra manual steps.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Playwright test credentials — see tests/.env.test.example
tests/.env.test
tests/.generated-*.json
