import PlaceholderNotice from "../../components/PlaceholderNotice";

// Sub-view opened from AdminOrdersTab — not routed directly.
const AdminOrderPreview = () => (
  <PlaceholderNotice title="Order Preview — coming soon" message="This Admin Portal feature is being re-ported and isn't wired up yet." />
);

export default AdminOrderPreview;
